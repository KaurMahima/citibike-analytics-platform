name: citibike-analytics
channels:
  - conda-forge

dependencies:
  - python=3.11
  - pandas>=2.2
  - pyarrow>=16
  - requests>=2.32
  - duckdb>=1.0
  - pyyaml>=6.0
  - pip
  - pip:
      - dagster>=1.7.0
      - dagster-webserver>=1.7.0
      - dbt-duckdb>=1.8.0