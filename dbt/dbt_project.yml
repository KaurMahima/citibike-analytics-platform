name: citibike_dbt
version: 1.0.0
config-version: 2 

profile: citibike_dbt 

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models: 
  citibike_dbt:
    staging:
      +materialized: view
    marts: 
      +materialized: table 